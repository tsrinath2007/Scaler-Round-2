name: Sync to Hugging Face
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true
      - name: Push to hub
        uses: huggingface/hub-sync@main
        with:
          github_repo_id: tsrinath2007/Scaler-Round-2
          huggingface_repo_id: tsrinath/Scaler-Round-2
          hf_token: ${{ secrets.HF_TOKEN }}
          repo_type: space
