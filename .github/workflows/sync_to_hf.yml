name: Sync to Hugging Face
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Push to Hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git push --force https://tsrinath:$HF_TOKEN@huggingface.co/spaces/tsrinath/Life-Support-ENV main
          git push --force https://tsrinath:$HF_TOKEN@huggingface.co/spaces/tsrinath/Life-Support-ENV main:master
